import {
  isSet,
  isUnset,
  isArray,
  extend,
  randStr,
  encodeJSON,
  decodeJSON,
  isFunction,
} from 'mytoolkit'
import drawGradient from './gradient'

export default function drawLine(chart, layer, s, index) {


}